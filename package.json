{
  "name": "funagig-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "start": "node server/src/index.js",
  "prestart": "npm install --prefix server",
  "install:all": "npm install --prefix client && npm install --prefix server",
  "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
  "build": "npm run build --prefix client --if-present",
  "test": "npm test --prefix client --if-present && npm test --prefix server --if-present"
}
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
